# מטל״מ v2
גרסה חדשה על Firebase (Firestore + Auth), נבנית במקביל למערכת הקיימת (`matlam`) שלא משתנה.
- `index.html` — האפליקציה
- `import.html` — העתקת נתונים מהמערכת הקיימת (קריאה בלבד)
- `firestore.rules` — כללי אבטחה (להדביק ב-Firebase Console)
